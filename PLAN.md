# DUST//REIGN 開發計劃

目標：在香港時間 9 月 10 日前，把這個零依賴的網頁 roguelike 射擊遊戲做成「開啟即玩」、有明確重玩理由、手機也能操作的可交付版本。所有版本只在本地 Git 管理，由使用者自行推送雲端。

## 目前垂直切片（先完成）

- Canvas 廢土戰場與鏽橙／氧化薄荷綠終端機 HUD
- WASD／方向鍵移動、滑鼠按住射擊、敵人追擊與三種敵型
- 擊殺掉落 scrap，升級時停下來三選一
- 波次計時、分數、生命、重開與觸控移動／射擊

## 9 月 9 日：可玩性與穩定性

### 增量 01／移動暴走（已排入）

- 觸發：Space 或觸控 DASH。
- 行為：沿目前移動或瞄準方向位移約 140px，短暫無敵，冷卻約 2.2 秒，避免變成常駐穿牆；Space 不再射擊。
- 驗證：碰撞中按下 Dash 會脫離並保留生命。

### 增量 02／版本留痕（已排入）

- 每個可交付版本在 CHANGELOG.md 記錄版本號、日期與改動摘要。
- 每個版本保留 screenshots/Vx.y.z-*.png，截圖在該版本完成後產生。
- 截圖只保留能說明新改動的畫面；測試用暫存檔不算版本資產。

### 增量 03／觸控導引（已排入）

- 觸發：手機／平板按下 FIRE。
- 行為：自動選最近的敵人作為瞄準點；沒有敵人時沿目前方向射擊。
- 邊界：只影響觸控輸入，桌面滑鼠瞄準不變；敵人上限低時用線性掃描即可。
- 驗證：窄螢幕能用移動＋FIRE 擊中第一個目標，無目標時不拋錯。

### 增量 04／高分保留（已排入）

- 觸發：每次遊戲結束時計算本局分數。
- 行為：用瀏覽器原生 `localStorage` 保存最高分，顯示在頂欄與結算畫面。
- 邊界：讀寫失敗時回退為 0，不阻塞遊戲；不做帳號同步。
- 驗證：刷新頁面後最高分仍在；新分數較低時不覆蓋；無 `localStorage` 仍可玩。

### 增量 05／精英訊號（已排入）

- 觸發：第 3 波起，低機率生成精英敵人。
- 行為：精英有更高生命與接觸傷害，外觀加護環；擊殺給更高分與更多 scrap。
- 邊界：不新增彈幕或新碰撞系統，沿用既有追擊／子彈流程，敵人上限不變。
- 驗證：第 1–2 波不出現；第 3 波後可生成、可擊殺、可掉落且不拋錯。

### 增量 06／連殺訊號（V0.0.5，本輪）

- 觸發：擊殺任一敵人後開始 4 秒連殺窗口；窗口內再次擊殺會疊加連殺層數。
- 行為：每層連殺提高該次擊殺分數 25%，最多顯示 8 層；窗口歸零後回到無連殺。
- 呈現：Canvas HUD 顯示 CHAIN xN 與剩餘窗口條，不遮擋既有操作；死亡／重開由新狀態自然清空。
- 邊界：只調整擊殺計分與畫面提示，不改敵人生命、掉落、輸入或敵人上限。
- 驗證：連續擊殺能疊層並提高分數；等待超過 4 秒後連殺歸零；node --check game.js 與 window.LunaGame.selfCheck() 通過。

### 增量 07／脈衝衝刺（V0.0.6，本輪）

- 觸發：按下 Space／觸控 DASH 時，在位移落點發出 0.24 秒脈衝。
- 行為：脈衝對落點 88px 內敵人造成 80% 武器傷害；仍沿用既有擊殺、掉落與連殺計分。
- 呈現：玩家周圍顯示向外擴散的氧化薄荷護環，讓無敵與攻擊窗口可讀。
- 邊界：不增加新敵型或碰撞系統，不改 DASH 冷卻／距離；沒有敵人時仍只位移。
- 驗證：DASH 後近敵受傷或死亡並掉落；脈衝逾時消失；node --check game.js 與 selfCheck 通過。

### 增量 08／安全暫停（V0.0.7，本輪）

- 觸發：已開始且不在升級／死亡畫面時按 P 或 Esc。
- 行為：凍結遊戲更新與輸入射擊，再按一次同鍵恢復；切窗 blur 仍只清輸入，不偷偷改變暫停狀態。
- 呈現：Canvas 顯示 SIGNAL PAUSED／PRESS P OR ESC TO RESUME，頂部狀態改為 PAUSED。
- 邊界：不共用升級 overlay，不改死亡、重開與觸控按鈕流程；開始畫面不會被誤切換。
- 驗證：暫停期間敵人位置與生命不變；恢復後繼續更新；node --check game.js 與 selfCheck 通過。

### 增量 09／精英超頻核心（V0.0.8，本輪）

- 觸發：擊殺精英敵人時，額外掉落一枚超頻核心；普通敵人不掉落。
- 行為：拾取後啟動 6 秒 OVERDRIVE，射速縮短至 62%、子彈傷害提高 50%；時間到恢復原值。
- 呈現：核心使用金色菱形 orb；Canvas HUD 顯示 OVERCLOCK 倒數與進度條。
- 邊界：沿用既有 orb 吸附／拾取流程，不新增背包、裝備或外部資源；精英既有分數與 XP 不變。
- 驗證：精英擊殺能產生核心；拾取後射擊屬性暫時提升；6 秒後恢復；node --check game.js 與 selfCheck 通過。

### 增量 10／REPAIR SCRAP（V0.0.9，本輪）

- 觸發：擊殺重型（brute）或精英敵人時，額外掉落一枚修復碎片；crawler／rusher 不掉落。
- 行為：拾取後立即回復最多 18 點 Hull，生命不會超過 maxHp；修復碎片不提供 XP、不觸發升級。
- 呈現：使用與 scrap／超頻核心可辨識的紅橙修復 orb 與拾取粒子，HUD／Canvas 清楚反映 Hull 回復。
- 邊界：沿用既有 orb 吸附、生命週期與碰撞流程，不新增背包、治療技能、最大生命提升或外部資源；既有 scrap、overdrive、分數與敵人上限不變。
- 驗證：brute／elite 擊殺能掉落修復碎片，crawler／rusher 不掉落；受傷拾取可回復且封頂 maxHp；與其他 orb 同時存在時不遺失 XP／效果；node --check game.js、selfCheck 與對應拾取畫面煙霧測試通過。

### 增量 11／WAVE BOUNTY（V0.0.10，本輪）

- 觸發：每次開始新波次時建立一個可見的擊殺目標與分數賞金；目標為 `5 + wave * 2`，賞金為 `120 + wave * 40`。
- 行為：每擊殺一名敵人累計 bounty 進度；達標只派發一次賞金並鎖定完成狀態；下一波或重開局重置進度與賞金。
- 呈現：沿用 mission rail 現有 objectiveText／objectiveProgress／threatIndex，顯示目標、進度、賞金與 LOW／HIGH／CRITICAL 威脅。
- 邊界：不新增敵人類型、碰撞或資源系統；賞金只改分數，不影響 XP、掉落、連殺與敵人上限。
- 驗證：擊殺能推進 bounty；達標只加一次分；進入下一波重置；DOM 進度與威脅字樣同步；node --check、selfCheck 與 bounty 功能畫面煙霧測試通過。

### 增量 12／BOUNTY SURGE（V0.0.11，本輪）

- 觸發：當本波 bounty 達標並結算一次性賞金時。
- 行為：除分數賞金外啟動 3 秒 OVERDRIVE surge；若已有更長超頻時間則保留較長值，不重複疊加。
- 呈現：沿用既有 OVERCLOCK HUD 倒數與進度條，賞金提示追加 SURGE 文案，讓高風險擊殺有短暫火力回報。
- 邊界：重用既有 `player.overdrive` 與射擊倍率，不新增 orb、武器、敵型、XP 或持久化資料。
- 驗證：bounty 達標同時加分並啟動 3 秒 surge；重複擊殺不再派獎；換波重置 bounty、overdrive 依既有倒數自然結束；node --check、selfCheck 與 surge 功能畫面煙霧測試通過。

### 增量 13／SALVAGE OVERFLOW（V0.0.12，本輪）

- 觸發：玩家已滿 Hull 時拾取 repair orb。
- 行為：repair orb 仍立即消耗；無法回復的 18 HP 轉為固定 `+12 SCORE`，受傷時維持原本回血且不給 XP。
- 呈現：HUD 狀態顯示 `REPAIR SCRAP FULL +12 SCORE`，Canvas 保留紅橙修復 orb 與拾取粒子。
- 邊界：沿用既有 repair orb 生命周期，不新增資源、背包、最大生命或持久化；不影響 bounty、連殺與 overdrive。
- 驗證：滿血拾取加分且只結算一次；受傷拾取只回血不加 overflow 分；node --check、selfCheck 與滿血 salvage 功能畫面煙霧測試通過。

### 增量 14／SCAV RADIO（V0.0.13，本輪）

- 觸發：bounty 達標、波次推進、repair 回復或滿血 overflow 時記錄一筆事件。
- 行為：沿用既有 `#runLog`，最新事件插到頂端，最多保留 5 筆，舊訊息自動移除。 重開時只清除本局動態事件，保留靜態提示。
- 呈現：事件含當局時間戳與簡短訊息；不遮擋戰場，桌面與窄螢幕沿用既有 rail 排版。
- 邊界：只更新現有 DOM feed，不新增後端、儲存、事件匯流排或遊戲規則；找不到 `#runLog` 時遊戲照常運作。
- 驗證：四類事件各寫入一次且上限 5 筆；node --check、selfCheck 與 SCAV RADIO 實際遊戲畫面煙霧測試通過。
### 增量 15／STORM CLOCK（V0.0.14，本輪）

- 觸發：每波最後 5 秒進入 STORM FRONT；沿用既有 `state.waveTime`，不新增持久化或敵型狀態。
- 行為：倒數低於 5 秒時將下一次敵人生成間隔乘以 `0.72`，仍受既有敵人上限限制，讓波末有短暫壓力峰值。
- 呈現：mission rail 新增 `STORM CLOCK` 倒數；最後 5 秒顯示 `STORM FRONT 05s`，其餘顯示下一次風暴倒數。
- 邊界：不覆寫 bounty／repair 狀態、不改波次長度、敵人生命、掉落或升級；窄螢幕沿用既有 rail 隱藏規則。
- 驗證：25 秒前為一般倒數，最後 5 秒顯示 STORM FRONT 並加快下一次生成；換波後倒數回到 30s；node --check、selfCheck 與 STORM CLOCK 功能畫面煙霧測試通過。

1. 對齊 HUD 與核心狀態：開始、升級、死亡、重開都只走一套流程。
2. 補上 dash（Space／觸控）與短暫無敵，讓移動策略不只是在繞圈。
3. 用原生 JS 做語法檢查、快速啟動測試與一輪瀏覽器煙霧測試；修正溢位、重複事件與手機觸控問題。
4. 以本地 Git 建立可回滾的里程碑提交。

## 9 月 10 日：小步增量（只加有回報的功能）

- 若核心循環穩定：加入一種高風險高回報的稀有掉落或精英敵人。
- 若效能或可讀性有問題：優先清理粒子／敵人上限、碰撞與小螢幕排版。
- 每次只改一個可驗證行為；若沒有明顯提升就不加入。

## 明確不做（本輪 YAGNI）

- 不引入框架、建置工具、帳號、後端或外部素材服務。
- 不做完整地圖編輯器、多人連線、商店與長篇劇情；等核心留存數據證明需要再開。

## 驗收條件

- 雙擊 `index.html` 或用任一靜態伺服器即可遊玩。
- 30 秒內能開始第一波，能射擊、升級、死亡並重開。
- 桌面與窄螢幕皆有可見焦點、可操作控制與不遮擋的 HUD。
- `node --check game.js` 通過，並能呼叫 `window.LunaGame.selfCheck()`。
